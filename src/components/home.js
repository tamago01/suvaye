import React from 'react';

const Home = () => {
  return (
    <div>
      <h1>Welcome to the Home Page</h1>
      <p>I tried to create separate pages while mapping the search context but encountered some errors so this is what I did instead.</p>
    </div>
  );
}

export default Home;
